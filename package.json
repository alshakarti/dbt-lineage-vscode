{
  "name": "dbt-lineage",
  "displayName": "dbt Lineage",
  "description": "Interactive dbt model lineage visualization — upstream/downstream DAG graph in the VS Code sidebar",
  "version": "0.1.0",
  "publisher": "Alshakarti",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_GITHUB_USERNAME/dbt-lineage-vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "dbt",
    "lineage",
    "dag",
    "data",
    "analytics engineering"
  ],
  "activationEvents": [
    "workspaceContains:**/dbt_project.yml"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "panel": [
        {
          "id": "dbt-lineage",
          "title": "DBT LINEAGE",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "dbt-lineage": [
        {
          "type": "webview",
          "id": "dbtLineage.view",
          "name": "Lineage",
          "when": "workspaceFolderCount > 0"
        }
      ]
    },
    "commands": [
      {
        "command": "dbtLineage.focusCurrent",
        "title": "dbt Lineage: Focus on Current Model",
        "icon": "$(target)"
      },
      {
        "command": "dbtLineage.showFullProject",
        "title": "dbt Lineage: Show Full Project Graph",
        "icon": "$(type-hierarchy)"
      },
      {
        "command": "dbtLineage.refresh",
        "title": "dbt Lineage: Refresh",
        "icon": "$(refresh)"
      }
    ],
    "menus": {},
    "configuration": {
      "title": "dbt Lineage",
      "properties": {
        "dbtLineage.enableLiveParser": {
          "type": "boolean",
          "default": false,
          "description": "Parse ref()/source() calls from .sql files in real-time on every save, without needing to run dbt. When disabled, only target/manifest.json is used."
        },
        "dbtLineage.defaultDepth": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Number of hops to show from the focused model (0 = unlimited)."
        },
        "dbtLineage.manifestPath": {
          "type": "string",
          "default": "",
          "description": "Override path to manifest.json (relative to workspace root). Leave empty to auto-detect from target/manifest.json."
        },
        "dbtLineage.colors.source": {
          "type": "string",
          "default": "#1e8a65",
          "description": "Color for source nodes."
        },
        "dbtLineage.colors.table": {
          "type": "string",
          "default": "#216595",
          "description": "Color for models materialized as tables."
        },
        "dbtLineage.colors.view": {
          "type": "string",
          "default": "#9e94d5",
          "description": "Color for models materialized as views."
        },
        "dbtLineage.colors.incremental": {
          "type": "string",
          "default": "#db704b",
          "description": "Color for incremental models."
        },
        "dbtLineage.colors.ephemeral": {
          "type": "string",
          "default": "#6b7280",
          "description": "Color for ephemeral models."
        },
        "dbtLineage.colors.materialized_view": {
          "type": "string",
          "default": "#b3013f",
          "description": "Color for models materialized as materialized views."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "build:prod": "node esbuild.mjs --production",
    "package": "npm run build:prod && npx @vscode/vsce package --no-dependencies",
    "vscode:prepublish": "npm run build:prod",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.webview.json"
  },
  "dependencies": {
    "@xyflow/react": "^12.3.6",
    "elkjs": "^0.9.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
